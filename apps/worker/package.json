{
  "name": "@trade-log/worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "node -e \"console.log('worker scaffold ready; BullMQ setup starts in Phase 4')\"",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "node -e \"console.log('lint placeholder: @trade-log/worker')\"",
    "test": "node -e \"console.log('test placeholder: @trade-log/worker')\""
  }
}
